# Binary Matrix Background

A small HTML demo that fills the browser window with animated binary digits. Hover over a digit to flip it between `0` and `1` with a neon highlight.

## Run

Open `index.html` in any modern web browser.
